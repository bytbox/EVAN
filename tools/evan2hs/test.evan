[[ Test of EVAN Syntax ]]

events <- Events .
each ev <- events {
	tracks <- Tracks ev .
	each tr <- tracks {
		m <- Mass tr .
	} ms -< m .
	[Total Mass] <- Sum ms .
} [TMass List] -< [Total Mass] .
hist <- Histogram [TMass List] .

return hist .

